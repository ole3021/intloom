/**
 * 用途：为 Specification 的需求分析与澄清提供模拟输入，供人工演练或测试使用。
 * todomvcIntents 包含 27 条按顺序补充需求的模拟 Intent；knowledge 提供澄清答复参考。
 * 累计需求以 todo.artifact.ts 为参考，不要求生成相同的 ID 或逐字一致的正文。
 * 这些输入不是 todo.records.ts 中两轮后续修改的 Intent；初始需求形成过程的 Record 未在示例中列出。
 * 模拟输入已纳入预设的澄清决定，不代表真实对话或运行记录。
 */
export const todomvcIntents = [
  // 1. 想做什么。
  `我想做个像 TodoMVC 那样的待办小工具，在浏览器里用，一份清单就够了。标题叫 todos，操作按钮用英文。不管用哪种语言或框架做，功能和用法都应该一样。`,

  // 2. 怎么添加。
  `上面放个输入框，提示 What needs to be done?。打开页面就能直接打字。我输入“买牛奶”按回车，就在最后添一条还没完成的待办，再把输入框清空，方便继续记下一条。不按回车就先别加进去。`,

  // 3. 输入的小细节。
  `标题两头多打的空格帮我去掉，中间的留着。空着或者只有空格就别添加。一样的标题可以记两次，当成两件独立的事，改一条、勾一条或删一条，都别影响另一条。`,

  // 4. 顺序和空清单。
  `待办按添加顺序排，每按一次回车只加一条。以后改名字或者勾完成，也别换位置。一条都没有时，只留标题和新增输入框，列表、全选和下面那排操作都收起来；添了第一条再显示，删光后再收起来。`,

  // 5. 标记完成。
  `每条旁边放个勾选框，勾上表示做完，取消勾选就表示还没做完。做完的标题划一道线，让我一眼分得清。点完马上变化，不用刷新，也别改标题、换位置或影响其他条目。`,

  // 6. 看还剩多少。
  `下面显示还有几件没做完，数字加粗。英文写成 1 item left、2 items left，零件就是 0 items left。添加、完成、重新打开或删除时跟着更新，改名字不用变。全部做完但还没删时也显示零，只有清单真的空了才把下面这排藏起来。`,

  // 7. 开始改标题。
  `双击标题就能在原来的位置改名字，做完的也能改。先把原标题放进去，让我直接接着输入。改的时候，这一条原来的文字、勾选框和删除按钮先藏起来。`,

  // 8. 保存修改。
  `改好按回车，或者点到别处，都保存并结束编辑。还是去掉两头的空格，中间保留。它仍然是原来那一条，做没做完、排在哪儿都别变。按回车后再点出去，也别重复处理，更不能误删。`,

  // 9. 不想改了。
  `按 Esc 就算了，退出编辑，恢复原来的标题。哪怕我把文字全删光了，按 Esc 也要留下原来那条。之后再点到别处，不能又把刚才取消的内容保存进去。`,

  // 10. 清空标题就删除。
  `不过，如果我把标题清空，或者只剩空格，然后按回车或点出去，就直接删掉这条，和 TodoMVC 一样。删除后数量和按钮也要跟着变，其他条目别动。`,

  // 11. 直接删除。
  `再给每条加个删除按钮，平时鼠标移到那条上面才出现，点一下就删。做完没做完都能删，只动我点的那条。剩下的内容和顺序不变，未完成数量、全选和清除按钮也一起更新。`,

  // 12. 一次勾选全部。
  `我还想一键把所有待办勾上，也能一键全部取消。勾完剩余数量就是零，全部取消后就是清单总数，标题和顺序都不变。只有每一条都完成时，全选才是勾着的；逐条勾完最后一条，它也自动勾上。重新打开一条或者再添一条，它就取消勾选。`,

  // 13. 清掉做完的。
  `有做完的事情才显示 Clear completed，点它删掉所有已完成的，没完成的原样保留，剩余数量也不变。清完就隐藏这个按钮，有待办留下就继续显示清单，删光才全部收起来。清空后别留下勾着的全选；再添加一条时，全选也不能是勾着的。`,

  // 14. 按完成情况查看。
  `下面再加 All、Active、Completed。All 看全部，Active 看没做完的，Completed 看做完的，选了哪个要看得出来。只是换个看法，仍然是同一份清单，内容和原来的先后顺序都别变。`,

  // 15. 在不同列表里操作。
  `在 Active 里勾完一条，它就从这里消失，在 Completed 或 All 里能找到；在 Completed 里取消完成则反过来。改标题也改的是同一条，回到 All 能看到。添加、修改、删除、全选或者清除后，列表马上更新，但别替我切换正在看的分类。`,

  // 16. 当前没有能显示的待办。
  `在 Completed 里也能添加，新添的还没完成，所以先不显示，但剩余数量要加一，去 Active 或 All 就能看到。有时候只是当前分类没有内容，清单里其实还有别的待办，这时只让列表空着，下面的数字和分类按钮都留着，好让我切回去。`,

  // 17. 看一部分时，哪些操作还是管全部。
  `下面的数字始终算整份清单。例如两条没做完、一条做完了，看哪个分类都显示 2 items left。全选也管全部，包括现在看不到的。Clear completed 同样清掉全部已完成的，所以在 Active 里，只要有做完的，也要显示这个按钮并能清掉它们。`,

  // 18. 可以直接打开某一类。
  `我希望地址也能看出正在看哪一类：#/ 是全部，#/active 是没做完的，#/completed 是做完的。没指定就看全部，直接打开某个地址就看那一类。用 #!/ 那种写法也行，几个入口统一就好。点分类时地址和列表一起变，已有待办别丢。`,

  // 19. 前进、后退和刷新。
  `浏览器的后退、前进也要好用。从 All 到 Active 再到 Completed，后退就依次回 Active、All，前进再回来，选中的按钮和列表都跟着变。刷新后仍按地址选分类，但这次不要求把刷新前的待办重新显示出来。地址只是选择前面那三种看法，以后分类规则改了，对应地址下的结果也要一起改。`,

  // 20. 自动保存在浏览器里。
  `每次操作完帮我自动保存，不用再点保存按钮。就放在当前浏览器的 localStorage 里。不同框架做的版本要分开存，名字按 TodoMVC 的约定用 todos-[framework]，把后面换成各自的框架名，别把另一个版本的清单覆盖了。`,

  // 21. 保存哪些结果。
  `每条待办的标题、做没做完和加入顺序都要记住，同名的两条也得分得清。新增保存整理过的标题和未完成的标记；改名就更新原来那条，别多出副本。单条或全部勾选、取消都保存。点删除、清空标题后按回车或点出去删除、清除已完成，也都从保存的清单里去掉；删光后就存空清单。`,

  // 22. 哪些东西还不能算保存好了。
  `保存要管整份清单，不能漏掉当前分类里看不到的条目。新增框里还没按回车的字、空输入，还有编辑到一半的文字都别存进去，也别记住“正在编辑”。按 Esc 取消后，存的仍然是原标题。现在只检查这些内容有没有保存正确，先不用做刷新后重新显示待办，也别因为这样就把已保存的内容清掉。`,

  // 23. 标题就是普通文字。
  `我打的标题就当普通文字处理。比如 <b>买牛奶</b>，要原样显示和保存，别变成加粗效果，更不能执行里面的代码。除了前面说的去掉两头空格，添加、修改、显示和保存时都别擅自改我的内容。`,

  // 24. 页面整体样式。
  `页面我想要干净、轻一点，背景用很浅的灰色，中间放一列白色的待办卡片，桌面上别铺满整个屏幕，窄屏也不要出现横向滚动。todos 放在卡片上方居中，字要很大、很细，用淡一点的红色，看起来比输入框和清单文字更突出。`,

  // 25. 输入框和清单行的样式。
  `新增输入框就放在白色卡片最上面，和下面的清单一样宽，字大一点，提示文字用浅灰色斜体；点进去时要看得出现在可以输入。每条待办单独占一行，留出舒服的间距，用细灰线分开，文字多了可以换行，不能盖住两边的操作。`,

  // 26. 待办状态和编辑样式。
  `每条左边用圆形的完成按钮，没做完时标题用清楚的深色；做完后按钮里显示绿色的勾，标题变成浅灰色并加删除线。删除按钮放在最右边，鼠标移到这一行才出现一个淡红色的叉，出现时不要把标题挤动。双击编辑后，编辑框就在原来标题的位置替换它，大小跟这一行协调，边框和焦点要明显。`,

  // 27. 页脚、提示和最终确认。
  `卡片底部那一排要紧凑：左边放剩余数量，中间放 All、Active、Completed，右边有需要时再显示 Clear completed。选中的分类用细的淡红色圆角边框圈出来，切换时整排不要跳来跳去。白色卡片下面可以有很轻的阴影和叠纸效果。卡片外面居中放 Double-click to edit a todo、作者或团队名字，以及指向 https://todomvc.com/ 的 Part of TodoMVC，小一点、浅灰一点，清单空了也保留。前面说的都按这个来，这一版没有需要留着以后再决定的事了。`,
] satisfies string[];

/**
 * 用途：供模拟调用方按当前问题选择答复，不作为整份 Stage Input 提交。
 * 内容覆盖初始需求，不包含后续搜索和防误删修改；具体使用边界见 usage。
 */
export const knowledge = {
  // 回答澄清问题时的使用方式。
  usage: [
    "供执行 todomvcIntents 时回答澄清问题使用。结合当前轮输入和已确认内容，选取相关要点作答；无需把整份知识背景作为一次新的 Intent 提交。",
    "背景包含最终目标的完整规则；不要仅因这里列出了某项能力，就自动把后续各轮的全部需求提前纳入本轮变更。",
    "知识中没有明确的行为应说明本示例未定义，不能自行补成确定需求；记录中的问答仍应反映实际发生的提问和回答。",
  ],

  // 目标范围与已确定的边界。
  scope: [
    "目标是 TodoMVC 各语言、框架共同实现的浏览器单清单待办应用；采用已明确描述的 TodoMVC 页面风格和英文界面文案。",
    "本知识对应 todo.artifact.ts 的基线内容，不包含 todo.records.ts 后续引入的标题搜索和防止空标题误删。",
    "本版空标题或纯空白标题在编辑时通过 Enter 或失焦提交，仍然删除待办；Escape 取消编辑时保留原待办。",
    "保留待办数据持久化，但不要求刷新后自动恢复并显示之前的待办。这不表示刷新时应删除已保存数据，也不取消地址所表达的筛选选择。",
    "工程目录、依赖安装、代码风格和具体框架内部实现不属于此业务知识的决定范围。",
  ],

  // 业务领域。
  domain: [
    "管理一份待办清单：记录要做的事情，修改内容，标记完成，移除不再需要的事项，按完成状态查看清单，并保存已提交的数据。每条待办具有独立身份、标题和完成状态。",
  ],

  // 功能职责。
  features: [
    "展示待办清单、空清单状态、未完成数量和必要的操作提示。",
    "把用户输入的有效标题加入清单，形成新的未完成待办。",
    "就地修改待办标题，支持提交、取消，以及通过清空标题删除待办。",
    "切换单条或整份清单的完成状态，并使完成状态在界面中一致呈现。",
    "删除指定待办，或一次清除整份清单中所有已完成待办。",
    "按全部、未完成、已完成查看同一份清单，并通过地址与浏览器导航表达当前筛选。",
    "自动持久化待办的已提交内容、完成状态和删除结果。",
  ],

  // 清单展示与空状态。
  listDisplay: [
    "页面提供一份待办清单和始终可用的新增入口；根据整份清单是否存在待办，显示或隐藏清单主体与操作页脚。",
    "页面显示标题 todos，顶部新增输入框提示 What needs to be done?；页面加载后该输入框获得焦点。",
    "当前清单没有任何待办时，隐藏清单主体（含全选控件）及操作页脚（计数、筛选和清除已完成）；标题和新增输入框仍然可见。",
    "加入第一条待办后，显示清单主体和操作页脚；删除最后一条待办后，重新进入空清单状态。",
    "清单按待办加入的先后顺序展示；修改标题或切换完成状态不改变顺序；筛选时保留可见待办之间的相对顺序。",
    "每条非编辑状态的待办显示标题和完成状态控件；已完成与未完成在视觉上可区分。",
    "清单实际有待办，但当前筛选没有匹配项时，仅待办列表为空；操作页脚和筛选入口仍显示，用户可以切换筛选。",
  ],

  // 页面与控件的可见样式。
  visualStyle: [
    "页面采用简洁轻量的单列布局：浅色中性背景上水平居中一张白色待办卡片；桌面宽度适中，窄屏下内容随可用宽度收缩且不产生横向滚动。",
    "todos 标题位于卡片上方并居中，使用明显大于输入框和清单文字的细体字及低饱和淡红色，与页面主体形成清晰层级。",
    "新增输入框位于白色卡片顶部并与清单等宽，采用较大的可读文字和浅灰色斜体提示；获得焦点时有可见的输入状态。",
    "白色卡片使用轻微阴影，底部呈现轻微叠纸层次；待办行保持一致的留白，以细的中性分隔线区分，长标题可换行且不遮挡两侧控件。",
    "未完成待办使用深色标题和圆形完成控件；已完成待办的控件显示绿色勾选，标题变为浅灰色并显示删除线。",
    "删除控件位于待办行最右侧，非悬停时隐藏，指针悬停该行时显示为淡红色叉号；显示与隐藏不改变标题或其他控件的位置。",
    "编辑状态以输入框在原标题位置就地替换显示内容，保持与待办行协调的尺寸，并以清晰边框和焦点状态表明正在编辑。",
    "操作页脚紧凑排列：剩余数量靠左，All、Active、Completed 位于中间，Clear completed 在需要时靠右显示；选中的筛选入口使用细的淡红色圆角边框，状态变化不造成整排明显跳动。",
    "卡片之外的操作提示、作者或团队信息和 TodoMVC 归属信息居中显示，使用小号、低强调的浅灰文字；清单为空时仍然可见。",
  ],

  // 新增待办。
  createTodo: [
    "用户在顶部输入框按 Enter 时，将去除首尾空白后的非空标题作为一条新的未完成待办，追加到清单末尾，并清空新增输入框。",
    "输入 买牛奶 并按 Enter 后，清单新增一条标题为 买牛奶 的待办，初始完成状态为未完成。",
    "输入标题首尾有空白时，保存前去除这些空白；标题中间的空格保留。",
    "空输入或仅含空白的输入按 Enter 后，不创建待办，不改变清单数量。",
    "成功新增后输入框内容清空，可以继续输入下一条；只输入文字而未按 Enter 时，不创建待办。",
    "连续新增多条待办时，每次有效提交只增加一条，并追加在已有待办之后。",
    "两次提交相同标题时保留两条独立待办；后续修改、完成或删除其中一条，不影响另一条。",
    "在 Completed 筛选下新增待办时，新待办仍加入完整清单并使未完成数量加一，但不出现在当前已完成列表中；切换到 All 或 Active 后可见。",
  ],

  // 编辑、提交与取消。
  editTodo: [
    "用户双击待办标题后可以就地编辑；按 Enter 或输入框失去焦点时提交，按 Escape 时取消；提交的标题去除首尾空白后为空时，删除该待办。",
    "双击未完成或已完成待办的标题后进入编辑状态，编辑框包含当前已提交标题并获得焦点。",
    "编辑期间隐藏该条待办原有的标题展示、完成状态控件和删除控件，以编辑框呈现标题。",
    "输入新的非空标题并按 Enter 后，保存标题并退出编辑状态；随后发生失焦也不重复提交或误删。",
    "输入新的非空标题后使编辑框失去焦点，保存标题并退出编辑状态，结果与按 Enter 提交一致。",
    "通过 Enter 或失焦提交时，去除标题首尾空白并保留中间空格；仅修改标题，不改变该待办的身份、完成状态或清单位置。",
    "标题清空或改为纯空白后，通过 Enter 或失焦提交，删除这条待办；其他待办保持不变。",
    "输入新标题后按 Escape，退出编辑并恢复编辑前已提交的标题；再使输入框失去焦点，也不会保存被取消的内容。",
    "将标题清空或改为纯空白后按 Escape，保留原待办和原标题，不执行删除。",
    "在 Active 或 Completed 筛选下编辑一条可见待办时，修改作用于同一条实际待办；切换到 All 后看到同样的修改结果。",
    "通过空标题提交删除待办后，未完成计数、筛选结果、全选状态、清除按钮和空清单展示按剩余待办更新。",
  ],

  // 单条完成状态。
  toggleTodo: [
    "用户通过单条待办的完成状态控件，在未完成与已完成之间切换；界面与相关统计立即反映新状态。",
    "勾选未完成待办后，该待办变为已完成，控件显示选中，标题显示完成样式，未完成数量减一。",
    "取消勾选已完成待办后，该待办变为未完成，完成样式消失，未完成数量加一。",
    "切换一条待办的完成状态，不修改其标题、身份或清单位置，也不改变其他待办的完成状态。",
    "完成状态变化后，当前筛选结果、全选控件和清除已完成按钮同步更新，无需重新加载页面。",
  ],

  // 批量完成与全选状态。
  toggleAll: [
    "用户可以将整份清单统一标记为已完成或未完成；全选控件反映整份清单是否全部完成，其操作范围包含被当前筛选隐藏的待办。",
    "清单至少有一条待办时，只有全部待办均已完成，全选控件才显示选中；只要存在未完成待办就显示未选中。",
    "将全选控件设为选中后，整份清单的每条待办均变为已完成，未完成数量为零。",
    "将全选控件取消选中后，整份清单的每条待办均变为未完成，未完成数量等于全部待办数量。",
    "逐条完成最后一条未完成待办时，全选控件自动选中；重新打开任意一条待办或新增未完成待办时，自动取消选中。",
    "当前为 Active 或 Completed 筛选时，批量操作仍作用于整份清单；操作后重新按当前筛选显示匹配项，保持原筛选不变。",
    "清除全部已完成待办后，不残留可见的全选选中状态；再次新增未完成待办时，全选控件显示未选中。",
  ],

  // 单条删除。
  deleteTodo: [
    "用户将指针悬停在一条非编辑状态的待办上时可见删除按钮，点击后删除该待办，无论其是否完成。",
    "指针悬停在待办条目上时显示该条目的删除按钮；点击后，该待办从完整清单及所有筛选结果中移除。",
    "删除未完成或已完成待办均可成功；仅删除被操作的那一条，其余待办的内容、状态和相对顺序保持不变。",
    "删除后更新未完成计数、全选状态及清除已完成按钮；若完整清单已无待办，则隐藏清单主体和操作页脚。",
  ],

  // 清除已完成。
  clearCompleted: [
    "完整清单中存在已完成待办时，提供 Clear completed 按钮，一次删除所有已完成待办并保留未完成待办。",
    "至少有一条已完成待办时显示 Clear completed；没有已完成待办时隐藏该按钮。",
    "点击后，所有已完成待办被删除；未完成待办的身份、标题、状态和相对顺序保持不变，未完成计数不变。",
    "在 Active 筛选下，即使已完成待办当前不可见，仍显示并可使用清除按钮；操作删除这些被隐藏的已完成待办。",
    "清除后按钮隐藏；若仍有未完成待办，则继续显示主体和页脚；若已无任何待办，则进入空清单状态。",
  ],

  // 未完成计数。
  remainingCount: [
    "操作页脚显示完整清单中未完成待办的数量，并使用正确的英文单复数；计数与当前筛选出的条目数量无关。",
    "清单非空时，剩余数量分别为 0、1、2 时，文案分别显示 0 items left、1 item left、2 items left；数字以加粗形式突出显示。",
    "切换 All、Active、Completed 筛选不改变未完成计数；例如有两条未完成、一条已完成时，三种筛选均显示 2 items left。",
    "新增、删除、单项完成或批量完成后，计数根据完整清单重新计算；编辑标题不改变计数，编辑为空并提交删除时按删除结果更新。",
    "全部待办已完成但尚未清除时仍显示 0 items left；完整清单变空时，计数随操作页脚一起隐藏。",
  ],

  // 状态筛选及跨视图行为。
  filterTodos: [
    "用户可以在 All、Active、Completed 三种视图间切换；筛选仅决定哪些待办可见，所有视图共同操作同一份完整清单。",
    "All 显示全部待办，Active 仅显示未完成待办，Completed 仅显示已完成待办。",
    "切换筛选后，仅当前筛选入口显示选中样式；筛选不新增、删除或修改实际待办。",
    "在 Active 中完成一条待办后，该条立即从当前视图消失，并可在 Completed 或 All 中找到。",
    "在 Completed 中将一条待办改为未完成后，该条立即从当前视图消失，并可在 Active 或 All 中找到。",
    "在任意筛选下新增、编辑、删除、批量完成或清除已完成后，当前视图按更新后的完整清单重新匹配，筛选选择本身保持不变。",
    "某个筛选没有匹配待办时显示空列表；只要完整清单仍非空，就可通过仍然显示的筛选入口返回其他视图。",
  ],

  // 地址、前进后退与刷新。
  filterNavigation: [
    "当前筛选由页面地址表达，支持直接访问与浏览器前进、后退；地址、选中的筛选入口和列表筛选规则保持一致。",
    "#/ 对应 All，#/active 对应 Active，#/completed 对应 Completed；官方允许采用等价的 #!/ 路由形式，各入口与导航须使用一致的形式。",
    "首次访问且地址未指定筛选时使用 All；直接访问三个合法筛选地址之一时，采用该地址对应的筛选规则。",
    "点击筛选入口后，地址更新为相应路由，同时更新选中入口与可见列表；本次页面内导航保留现有待办内容。",
    "依次从 All 切换到 Active、Completed，再使用浏览器后退时，依次回到 Active、All；前进时按历史记录再次进入相应筛选，列表与选中入口同步变化。",
    "在合法筛选地址刷新页面后，采用地址对应的筛选规则；若操作页脚可见，其选中入口与地址一致。本项只验收筛选状态，不要求恢复刷新前的待办。",
  ],

  // 保存内容与提交边界。
  persistTodos: [
    "待办发生有效提交后自动持久化完整清单的最新数据，包括待办身份、标题、完成状态及清单顺序；删除结果同步保存，无需用户执行额外保存操作。",
    "新增有效待办后，可从持久化数据中核对到该待办的独立身份、去除首尾空白后的标题和未完成状态；连续新增多条时，保存结果能够表达加入顺序。",
    "通过 Enter 或失焦提交标题修改后，持久化数据中同一待办的标题更新，身份和完成状态不变，不产生额外副本。",
    "切换单条完成状态或执行批量完成、批量取消完成后，持久化数据中所有受影响待办的完成状态与提交结果一致。",
    "直接删除、提交空标题或清除已完成后，对应待办从持久化的有效清单中移除；删除最后一条后，保存结果表示空清单，不残留旧的有效待办。",
    "编辑中的临时标题和是否正在编辑不作为已提交待办数据保存；按 Escape 取消后，持久化标题保持为编辑前的值。",
    "新增输入框中尚未提交的文字，以及被拒绝的空标题，不成为持久化待办。",
    "在筛选状态下提交变化时，保存完整清单的最新状态；被筛选隐藏且未被操作的待办仍保留，不因当前不可见而从存储中丢失。",
  ],

  // 帮助提示与项目归属。
  appInformation: [
    "待办操作区域之外提供双击编辑的提示及 TodoMVC 项目归属信息，帮助用户理解编辑方式和示例来源。",
    "页面展示 Double-click to edit a todo，提示用户可以通过双击标题编辑待办。",
    "页面展示当前实现的作者或团队信息，并提供指向 https://todomvc.com/ 的 TodoMVC 项目入口；作者文案由具体实现填写。",
    "清单为空、操作页脚隐藏时，编辑提示和项目归属信息仍然可见。",
  ],

  // 适用于整个应用的约束。
  constraints: [
    "整个应用采用已定义的 TodoMVC 页面布局、视觉状态和交互语义；不同语言或框架的实现应保持相同的用户可见行为、英文操作文案与可辨识的视觉层级。",
    "待办数据保存在当前浏览器的 localStorage 中，以 todos-[framework] 命名约定区分不同框架实现的数据，其中 [framework] 替换为实现名称；一个实现的新增、修改和删除不得覆盖另一个实现的清单。持久化验收直接核对已提交的存储结果。",
    "待办标题在新增、编辑、列表展示和保存过程中始终作为用户文本处理。除规定的首尾空白处理外，保留标题内容；例如输入 <b>买牛奶</b> 时，显示并保存这段文字本身，不将其解释为页面标记或可执行内容。",
  ],

  // 需求之间的关系。
  relations: [
    "地址导航依赖状态筛选：source 为地址导航要求，target 为状态筛选要求，关系为 depends_on。",
    "地址导航选择的是三种已定义筛选之一，其结果依赖 All、Active、Completed 的筛选语义；筛选规则调整时须同步检查路由选择与浏览器导航的结果。",
  ],

  // 需求组织与示例约定。
  artifactConventions: [
    "全部功能属于同一个待办管理领域；每条要求归属其负责的功能，验收标准归属对应要求。全局约束独立保存，不挂到单个功能下。",
    "本基线的领域、功能、要求、约束和关系全部有效（active），没有退役内容；没有已确认需要暂缓的问题，deferreds 为空。",
    "目标文件中的需求 ID 用于标识需求、验收及其关联，不是应用中每一条待办的数据 ID；生成过程维护稳定身份，不要求用户在答复中复述这些编号。",
    "基线的 record_refs 为空，是因为该示例省略初始历史记录。真实执行 Intent 时应按记录维护规则生成来源引用，不应为复刻示例而抹去真实记录。",
  ],
} satisfies Record<string, string[]>;
